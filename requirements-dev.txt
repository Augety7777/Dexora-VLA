# Developer / CI requirements (the corresponding production deps live in
# requirements.txt and pyproject.toml).
#
# Install with:   pip install -r requirements-dev.txt
# Or via PEP 517: pip install -e ".[dev]"

pytest>=8.0
pytest-cov>=4.1
ruff>=0.5.0
black>=24.4
pre-commit>=3.7
build>=1.2
